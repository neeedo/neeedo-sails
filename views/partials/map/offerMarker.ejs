<div id="offerMarker">
  <h4 class="media-heading title"> {{tags}}</h4>
  <p>
    {{price}}
  </p>
  <p>
    <img src="{{image}}" alt="{{imagetitle}}" />
  </p>
  <p>
    <a href="{{viewUrl}}" title="Details">Details</a>
  </p>
</div>

