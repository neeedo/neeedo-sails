<div id="fileupload-files">
  <ul>
    <% if (undefined !== images && typeof images.getImages === 'function') { images.getImages().forEach(function(image) { %>
    <li class="previewImages"><img src="<%= FileService.filterGetImageUrl(image.getUrl()) %>"
                                   alt="<%= image.getFileName() %>">
      <div class="fileupload-delImg icon-cancel-circle" data-imageName="<%= image.getFileName() %>"></div>
    </li>
    <% }) } %>
  </ul>
</div>
