<li class="listItem ">
  <% // I'm actually not sure why we need to unserialize again, but the following line is really important when this partial is used during an AJAX action, do not remove!         %>
  <% demand = ApiClientService.newDemand().loadFromSerialized(demand) %>

  <div class="media-body">

    <div class="row">
      <div class="col-xs-6">

        <label class="media-heading " for="mustTags"><%= i18n('Must have`s') %></label>

        <ul id="mustTags">
          <% demand.getMustTags().forEach(function(mustTags){ %>
          <li>
            <%= mustTags %>
          </li>
          <% }) %>
        </ul>

      </div>
      <div class="col-xs-6">
        <label for="shouldTags"><%= i18n('Should have`s') %></label>

        <ul id="shouldTags">
          <% demand.getShouldTags().forEach(function(shouldTags){ %>
          <li>
            <%= shouldTags %>
          </li>
          <% }) %>
        </ul>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-6">

        <label for="dist"><%= i18n('Distance') %></label>

        <p id="dist"><%= i18n('Distance') %>:<%= demand.getDistance() %></p>

      </div>
      <div class="col-xs-6">
        <label for="price"><%= i18n('Price') %></label>

        <p id="price"><%= i18n('Price') %>:<%= demand.getPrice().getMin() %> - <%= demand.getPrice().getMax() %><span
            class="glyphicon glyphicon-euro" aria-hidden="true"></span></p>
      </div>
    </div>

  </div>
  <%- partial('../partials/crudButtonsDemands.ejs', {demand: demand}) %>

</li>
